name: Tests

on:
  pull_request:
    branches: [ "master" ]
  workflow_call:

jobs:
  tests:
    name: Run tests
    
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: |
          ls -lia
        name: List Files
